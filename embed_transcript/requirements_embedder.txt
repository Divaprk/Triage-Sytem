# Requirements for symptom_embedder.py
#
# Model: sentence-transformers/all-MiniLM-L6-v2 (~80MB)
#
# sentence-transformers pulls in torch (CPU build) and transformers automatically.
# On Raspberry Pi 5, install torch CPU-only first to avoid downloading the CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Future optimisation: export the model to ONNX and quantize with:
#   pip install optimum[onnxruntime]
# This can significantly reduce inference latency on CPU-only hardware.

sentence-transformers>=2.7.0
scikit-learn>=1.3.0
numpy>=1.24.0
